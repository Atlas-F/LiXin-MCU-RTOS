## **功能设计：**

按键控制LED

不同的按键指令实现不同的LED翻转效果



短按实现led 单次翻转

长按实现led指定次数 闪烁



## **方案：**

双边沿中断触发效果，通过两次中断触发之间的时间差来判断

消息队列通知按键线程和LED线程





**LED方案：**

接收key 的指令控制闪烁或者是翻转，或者是不同的类型





**key 方案：**

通过中断的双边沿发送信息到消息队列

检测特殊的中断边沿触发类型，例如：先降后升，或者相反

检测先降后升为有效类型，计算该两次中断之间的时间，根据时间判断长按或者短按





**通信：**

两个消息队列：

中断和按键：

中断收集按键的触发信息：电平、tick 计数

按键接收





**按键和LED：**

按键发送不同的操作类型指令

LED接收执行







**BSP方案：**

设置led 操作返回状态枚举

设置led 操作类型枚举



设置key操作返回状态枚举

设置key 操作类型枚举，但是建立在单次操作的基础上



**单元测试方案：**







## **优化：**

近似零拷贝的方式来缩短中断的执行时间
